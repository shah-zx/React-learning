import React from 'react';

class Duplicate extends React.Component {
    const = {message} = this.props;
    render() {
        return (
             <h1>{message}</h1>
        )
    }
}

export default  Ucomponent(Duplicate)

